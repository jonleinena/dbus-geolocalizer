{
  "name": "dbus-visualizer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --production=false && npm run build && cd ../backend && npm install --production=false && npm run build",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=18"
  }
}

